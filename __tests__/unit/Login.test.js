import { shallowMount } from '@vue/test-utils';
import Login from '@/views/Login';

describe('Login', () => {
  test('é uma instacia do Vue',()=> {
    // const wrapper = shallowMount(Login);

    // expect(wrapper.isVueInstance()).toBeTruthy();
  })
});
